<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FatwaOct2022.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FatwaOct2022"
    Shell.FlyoutBehavior="Disabled"
    FlowDirection="RightToLeft">

    

    <ShellContent
        Title="الرئيسة"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="عن المجمع"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="قسم الفتوى"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="سؤال فقهي؟"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
